# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=control_gastos_db
DB_USER=cg_user
DB_PASS=jaime789521

# JWT Configuration
JWT_SECRET=changeme_to_something_secure

# Server Configuration
PORT=3000

# ============================================
# INSTRUCCIONES
# ============================================
# 1. Copia este archivo a .env:
#    cp .env.example .env
#
# 2. Si usas Docker (recomendado):
#    - Mantén las credenciales por defecto
#    - Ejecuta: ./docker-start.sh
#    - Esto levantará PostgreSQL en puerto 5432
#
# 3. Si usas PostgreSQL local:
#    - Crea la base de datos: createdb control_gastos_db
#    - Modifica DB_USER y DB_PASS según tu configuración local
#    - Aplica el schema: psql -U tu_user -d control_gastos_db -f schema.sql
#
# 4. Cambia JWT_SECRET a algo seguro en producción
#
# 5. Inicia la aplicación:
#    npm run dev:all
